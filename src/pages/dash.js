import Header from '../pages/menu.js';

export default ()=>{
    let dash = document.createElement('div');
        dash.setAttribute('class', 'dashboard');










dash.appendChild(Header());

return dash
};